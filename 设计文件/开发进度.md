# 开发进度

> 用于在会话压缩、切换设备或中断后，快速恢复当前开发状态。
> 规则：每完成一项任务后更新一次；未完成项写清“进行中 / 下一步”。

---

## 当前总目标
在 `HarmonySSH22` 中完成 HarmonyOS NEXT 前端重写，阶段一先用 Mock 跑通：

- Login 登录页
- Home 首页 / 服务器列表
- ServerForm 服务器新增/编辑页
- Terminal SSH 终端页
- Sftp 文件管理页
- Settings 设置页

要求：
- 能编译
- 能运行
- 能演示
- 单入口 `Index.ets`
- 暂不接真实后端

---

## 已完成

### 工程与构建
- 已在 `HarmonySSH22` 上完成基础工程重建。
- 已完成单入口 `Index.ets` 状态路由结构。
- 已多次通过本地构建验证：
  - `hvigorw.bat --no-daemon assembleHap`
- 当前构建结论：**可以成功编译打包**。

### 首页（Home）
- 服务器列表从顶部向下排列。
- `新增服务器` 按钮已改为固定在底部区域。
- 首页基础跳转逻辑已通。

### 图标
- 应用图标已改为使用：
  - `entry/src/main/resources/base/media/logo.png`
- `module.json5` 中：
  - `icon` → `$media:logo`
  - `startWindowIcon` → `$media:logo`
- 当前图标链路已经通过本地构建验证。

### SSH 页面（当前已完成）
- 已完成基础 SSH mock 页面。
- 已完成顶栏基础收缩。
- 顶栏标题已绑定当前选中的服务器：
  - 优先显示 `ServerItem.name`
  - 为空时回退到 `username@host`
- 已将底部快捷键改成更轻量的单排工具条。
- 已去掉底部独立输入框。
- 已加入终端区内透明输入宿主，用于承接输入法输入。
- 已将 Terminal 页面切换为独立深色工作区，尽量减少白边干扰。
- 已把终端输出顶部间距压到最小，尽量从最顶部开始显示。
- 当前这版已经本地构建通过，等待真机进一步确认输入法 / 铺满 / 顶部贴边的最终体验。

### SFTP 页面（已完成的部分）
- 已完成基础 SFTP mock 页面。
- 已支持目录切换基础流程。
- 已开始铺设以下 mock 交互：
  - 文件预览
  - 删除
  - 重命名
  - 下载
  - 批量选择 / 打包下载
- **注意：SFTP 页面还没有最终收口，后续按单独任务继续细化。**

---

## 当前进行中

### 下一阶段准备
SSH 当前版本已进入“真机验证后再微调”的阶段。
接下来按 `下一步任务.md` 的顺序，开始做：

1. 登录页面
2. FTP 页面
3. 设置页面
4. 服务器管理页面

说明：
- 每个任务保持独立，不随意修改已完成部分。
- 每完成一项任务：
  1. 本地编译验证
  2. 提交并 push 到 GitHub
  3. 给博士汇报一次

---

## 下一步任务顺序（来自 `下一步任务.md`）

### 任务1：修改登录页面
参考：
- `设计文件/登录页面设计参考.png`
- `设计文件/登录页面设计说明.md`

### 任务2：修改 ftp 页面
参考：
- `设计文件/ftp页面设计参考图.jpg`
- `设计文件/fpt页面设计说明.md`

### 任务3：修改设置页面
参考：
- `设计文件/登录页面设计参考.png`
- `设计文件/登录页面设计说明.md`

### 任务4：修改服务器管理页面
参考：
- `设计文件/服务器管理页面设计参考.png`
- `设计文件/服务器管理页面设计说明.md`

---

## 当前已确认的设计偏好

### SSH 页面
- 不要底部独立输入框
- 输入应融入终端区域
- 顶栏显示当前服务器名，不是固定文案
- `在线测试` 这一栏右侧放绿色小圆点
- 黑色终端区尽量铺满屏幕中间主体区域
- 底部快捷键区保留，但要轻量化、工具条化

### 首页
- `新增服务器` 按钮固定在底部
- 列表区域独立滚动

### 工作方式
- 一次只改一小块
- 每次改完先本地编译
- 能真机验证了再汇报

---

## 恢复开发时优先读取
如果后续会话被压缩或中断，恢复时优先读取：

1. `设计文件/开发进度.md`
2. `设计文件/下一步任务.md`
3. 当前任务对应的设计参考图 / 设计说明

---

## 最后更新时间
- 2026-03-20 23:18（Asia/Shanghai）
